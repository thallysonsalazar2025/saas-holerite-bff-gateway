services:
  mysql-db:
    image: mysql:8.0
    container_name: holerite-mysql-db
    restart: always
    ports:
      - "3306:3306"
    environment:
      MYSQL_ROOT_PASSWORD: rootpassword
      MYSQL_DATABASE: holerite_db
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data: